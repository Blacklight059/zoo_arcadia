{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h3 class="m-3">Service</h3>

    <table class="table m-2">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn-back m-3" href="{{ path('app_service_index') }}">Retour à la iste</a>

    <a href="{{ path('app_service_edit', {'id': service.id}) }}">editer</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %}
