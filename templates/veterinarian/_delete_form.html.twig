<form method="post" action="{{ path('app_veterinarian_delete', {'id': veterinarian.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce vétérinaire?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veterinarian.id) }}">
    <button class="btn">Delete</button>
</form>
