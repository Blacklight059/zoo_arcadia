{% extends 'base.html.twig' %}

{% block title %}FoodConsumption{% endblock %}

{% block body %}
    <h1>FoodConsumption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ food_consumption.id }}</td>
            </tr>
            <tr>
                <th>DateTime</th>
                <td>{{ food_consumption.dateTime ? food_consumption.dateTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FoodType</th>
                <td>{{ food_consumption.foodType }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ food_consumption.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_food_consumption_index') }}">back to list</a>

    <a href="{{ path('app_food_consumption_edit', {'id': food_consumption.id}) }}">edit</a>

    {{ include('food_consumption/_delete_form.html.twig') }}
{% endblock %}
